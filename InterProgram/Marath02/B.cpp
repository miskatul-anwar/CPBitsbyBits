#include <iostream>
#include <string>
#define fast_io                                                                \
  std::ios_base::sync_with_stdio(false);                                       \
  std::cin.tie(nullptr);

using str = std::string;

int main() {
  fast_io str cmd1, cmd2;
  std::cin >> cmd1 >> cmd2;

  return 0;
}
